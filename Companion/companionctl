#!/bin/sh
set -eu

SCRIPT_DIRECTORY=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if command -v python3.11 >/dev/null 2>&1; then
    PYTHON_COMMAND=python3.11
else
    PYTHON_COMMAND=python3
fi
exec "${PYTHON_COMMAND}" "${SCRIPT_DIRECTORY}/deploy/companionctl.py" "$@"
